@model List<Product>

@{
    ViewData["Title"] = "Products for Scrapers";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h1>Product for Scraping</h1>
    <div class="btn-group" role="group">
        <form asp-action="PauseAllScraping" method="post" class="d-inline">
            <button type="submit" class="btn btn-warning" title="Pause all background scraping">
                <i class="bi bi-pause-circle"></i> Pause All
            </button>
        </form>
        <form asp-action="ResumeAllScraping" method="post" class="d-inline">
            <button type="submit" class="btn btn-success" title="Resume all background scraping">
                <i class="bi bi-play-circle"></i> Resume All
            </button>
        </form>
        <a asp-action="Create" class="btn btn-primary">
            <i class="bi bi-plus-circle"></i> Add New Product
        </a>
    </div>
</div>

@if (!Model.Any())
{
    <div class="alert alert-info">
        <h4>No products configured yet</h4>
        <p>Get started by adding your first product to monitor.</p>
        <a asp-action="Create" class="btn btn-primary">Add Product</a>
    </div>
}
else
{
    <div class="table-responsive">
        <table class="table table-striped table-hover">
            <thead class="table-dark">
                <tr>
                    <th>ID</th>
                    <th>Adapter</th>
                    <th>Product Name</th>
                    <th>Sizes</th>
                    <th>Status</th>
                    <th>Check Interval</th>
                    <th>Last Updated</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var product in Model)
                {
                    <tr>
                        <td>@product.Id</td>
                        <td>
                            <span class="badge bg-info">@product.Adapter.Name</span>
                        </td>
                        <td>
                            <a href="@product.ProductPageUrl" target="_blank" class="text-truncate d-inline-block" style="max-width: 300px;" title="@product.ProductPageUrl">
                                @product.Name
                            </a>
                        </td>
                        <td>
                            @foreach (var sku in product.ProductSkus)
                            {
                                if (sku.TemporaryDisabled)
                                {
                                    <span class="badge bg-secondary text-decoration-line-through me-1" title="Temporarily disabled">@sku.Name</span>
                                }
                                else
                                {
                                    <span class="badge bg-primary me-1">@sku.Name</span>
                                }
                            }
                        </td>
                        <td>
                            <form asp-action="ToggleEnabled" asp-route-id="@product.Id" method="post" class="d-inline">
                                @Html.AntiForgeryToken()
                                <button type="submit" class="btn btn-sm @(product.IsEnabled ? "btn-success" : "btn-secondary")">
                                    @(product.IsEnabled ? "✓ Enabled" : "✗ Disabled")
                                </button>
                            </form>
                        </td>
                        <td>Every @product.CheckIntervalSeconds sec</td>
                        <td>
                            @if (product.UpdatedAt.HasValue)
                            {
                                @product.UpdatedAt.Value.ToLocalTime().ToString("g")
                            }
                            else
                            {
                                <span class="text-muted">Never</span>
                            }
                        </td>
                        <td>
                            <div class="btn-group" role="group">
                                <a asp-action="Details" asp-route-id="@product.Id" class="btn btn-sm btn-info">Details</a>
                                <a asp-action="Edit" asp-route-id="@product.Id" class="btn btn-sm btn-warning">Edit</a>
                                <form asp-action="Delete" asp-route-id="@product.Id" method="post" class="d-inline"
                                      onsubmit="return confirm('Are you sure you want to delete this product? This will also delete all notification history.');">
                                    @Html.AntiForgeryToken()
                                    <button type="submit" class="btn btn-sm btn-danger">Delete</button>
                                </form>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    <div class="mt-4">
        <a href="/hangfire" target="_blank" class="btn btn-secondary">
            View Hangfire Dashboard →
        </a>
    </div>
}
